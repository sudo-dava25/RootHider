<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.RootHider">

        <!-- Marks this APK as an Xposed/LSPosed module -->
        <meta-data
            android:name="xposedmodule"
            android:value="true" />
        <meta-data
            android:name="xposeddescription"
            android:value="Hides root / KernelSU / APatch traces from selected apps" />
        <meta-data
            android:name="xposedminversion"
            android:value="82" />
        <!-- LSPosed-specific scope-aware API flag -->
        <meta-data
            android:name="xposedscope"
            android:resource="@array/xposed_scope_hint" />

        <activity
            android:name=".ConfigActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
